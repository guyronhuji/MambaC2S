torch>=2.0
numpy<2.0  # fcsparser (used by PyCytoData) is not yet compatible with NumPy 2.x
pandas
scikit-learn
umap-learn
anndata
fcsparser
pyyaml
omegaconf
matplotlib
seaborn
tqdm
PyCytoData  # primary Levine32 loader (downloads + caches dataset automatically)
tabulate    # used by summarize_results.py for markdown tables
# Mamba SSM kernels — install the right variant for your platform:
#   macOS Apple Silicon:  pip install mamba-ssm-macos
#   Linux + CUDA:         pip install mamba-ssm --no-build-isolation  (done by runpod/setup_pod.sh)
