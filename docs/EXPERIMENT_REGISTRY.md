# Experiment Registry

Track all completed experiments.  Update this file after each run.

> Generated by `scripts/summarize_results.py` — or update manually.

---

## How to Add an Entry

After running a full experiment:

1. Run `python scripts/summarize_results.py` to regenerate `outputs/summary.csv`.
2. Copy the relevant row into the table below.
3. Add notes in the **Notes** column for anything worth remembering.

---

## Registry Table

| exp_id | model_type | tokenization | d_model | n_layers | best_val_loss | ARI | NMI | knn_purity | val_perplexity | checkpoint | notes |
|--------|-----------|--------------|---------|----------|---------------|-----|-----|-----------|----------------|-----------|-------|
| *(no experiments run yet)* | | | | | | | | | | | |

---

## Planned Experiments

| # | model_type | tokenization | status |
|---|-----------|--------------|--------|
| 1 | transformer | rank_only | pending |
| 2 | transformer | strength_only | pending |
| 3 | transformer | hybrid | pending |
| 4 | mamba | rank_only | pending |
| 5 | mamba | strength_only | pending |
| 6 | mamba | hybrid | pending |

---

## Notes

- All experiments use seed=42 and the same `split_manifest.json`.
- Baseline (marker space, no model) should be computed for UMAP comparison.
- Test set evaluation is run once per final model only.
